/**
 * Config.gs — 這一個檔案就是「你們組織的設定」。
 * ------------------------------------------------------------------
 * 使用方式：把這個檔案複製成 Config.gs（Apps Script 裡檔名就叫 Config），
 * 逐欄改成你們的內容。Code.gs 與 Index.html 一行都不必動。
 * 每個欄位旁邊都有說明。改完要重新「部署 → 新版本」才會生效。
 * ------------------------------------------------------------------
 */

var CFG = {
  /* ---------- 資料來源 ---------- */
  // 班表所在的 Google 試算表 ID（網址 /d/ 與 /edit 之間那一串）。
  // 第一次建置可以先留空白：在編輯器執行 setupSpreadsheet() 會自動建一份新試算表並印出 ID，
  // 把 ID 填回這裡再執行一次，就會建好第一個月份分頁。
  SPREADSHEET_ID: '',
  LOG_SHEET: '_操作紀錄',        // 隱藏的操作紀錄分頁名稱

  /* ---------- 帳號與登入 ---------- */
  DEFAULT_PASSWORD: '0000',    // 所有人的初始密碼；沒改過的人一律用這組（務必公告大家改掉）
  TOKEN_DAYS: 30,              // 登入後記住幾天
  LOGIN_MAX_FAIL: 10,          // 連續密碼錯誤幾次就鎖定
  LOGIN_LOCK_MIN: 10,          // 鎖定幾分鐘
  LOGIN_FAIL_WINDOW_MIN: 10,   // 超過幾分鐘沒再錯就把次數歸零

  // 超級管理者：擁有全部後臺功能，而且只有他們能把個別功能「授權」給別人。
  // 填順位表裡的姓名，或下面 STAFF_ACCOUNTS 的名稱都可以（例如讓會務人員當超級管理者）。
  ADMIN_NAMES: ['王小明'],

  // 不參與排班的帳號（會務人員、幹事…）：有自己的密碼、可以查看所有班表，
  // 但不能填班／交棒。要讓他們管理後臺，登入超級管理者用「權限授權」勾給他。
  // seq 是登入頁的代號，請用順位表用不到的字母（順位藥師由 A 往後編）。
  STAFF_ACCOUNTS: [
    { seq: 'Z', name: '會務人員' }
  ],

  /* ---------- 排班規則的數字 ---------- */
  DEADLINE_DAY: 15,            // 每月幾號前完成次月排班
  OPEN_DAY: 5,                 // 每月幾號公布次月可排班日
  MAX_SHIFTS_PER_MONTH: 1,     // 每人每月至多幾班
  MAX_CONSECUTIVE_DAYS: 3,     // 最多連續排班幾日（勞基法：不得連續 4 日以上）
  LONG_HOLIDAY_MIN: 4,         // 連續幾個可排班日以上算「長連假」（觸發優先權規則）

  /* ---------- 畫面上的文字（全部可改） ---------- */
  UI: {
    orgName: '社團法人○○市藥師公會',            // 頁尾與電腦版頁首用的全名
    orgShort: '○○藥師公會',                      // 瀏覽器分頁標題用的短名
    systemName: '假日急診排班系統',               // 系統名稱
    titleDesktop: '社團法人○○市藥師公會　假日急診排班系統',   // 電腦版頁首標題
    titleMobile: '藥師 假日排班',                              // 手機版頁首標題（字要少）
    venueLine: '○○醫院　早班 08:00–16:00／晚班 16:00–24:00',   // 頁首第二行（電腦版）
    shifts: {                                      // 兩個班別的名稱與時段（系統固定兩班制）
      am: { label: '早班', hours: '0800~1600' },
      pm: { label: '晚班', hours: '1600~2400' }
    },
    contactShort: '公會窗口',                      // 各種訊息裡「請洽○○」的稱呼
    contactLine: '排班規則或班表異動請洽公會窗口　(0X)XXXX-XXXX　○○○ 幹事',   // 頁尾聯絡方式
    forgotLine: '忘記密碼請洽公會窗口 (0X)XXXX-XXXX ○○○ 幹事重設。',            // 登入頁的忘記密碼說明
    loginScope: '本系統為公會內部使用，僅限排班藥師。',                            // 登入框上方一行
    confidentialNote: '本系統為公會內部使用，內容請勿外傳或截圖分享',              // 頁尾紅字
    rulesTitle: '排班規則摘要',                                                    // 規則區塊標題
    rules: [                                       // 規則清單，一項一行，顯示在班表下方（純說明，程式不解析）
      '順位採輪替制，每月順位依序向前遞補一位。',
      '每月 5 號公布次月可排班天數並開始填班；每人至多 1 天思考時間，逾期視同放棄，由下順位者接續。務必於每月 15 號前完成。',
      '每人至多填寫 1 班；當月無法支援可不填。',
      '4 天以上連假之班別，優先以前三個月排班最多者填寫；班次相同者依順位決定。',
      '可自行與優先順位者協調代為填班，待全體班表完成後再調整換回。',
      '勞基法：每日僅得排一班、不得連續排班 4 日以上（最多 3 日）、不得晚班接隔日早班。',
      '新進人員初始順位自最後開始。',
      '未盡事宜悉依公會窗口之解釋與協調為準。'
    ]
  },

  /* ---------- 只在第一次建置時用（setupSpreadsheet） ---------- */
  // 第一個月份分頁的順位名單，依順序給代號 A、B、C…。試算表已經有月份分頁時不會用到。
  SETUP_ROSTER: ['王小明', '陳小美', '林小華']
};

/**
 * 週日以外的國定假日／補假候選清單（第一次建立 _可排班日 分頁時帶入）。
 * 一律預設「否」（不開放）；哪一天要開診由窗口在 _可排班日 分頁改成「是」。
 * 請依行政院人事行政總處公告填當年度的日期（這裡是 2026 年的範例）。
 */
var HOLIDAYS = [
  ['2026-01-01', '中華民國開國紀念日'],
  ['2026-02-14', '春節連假（4 日以上）'],
  ['2026-02-16', '春節連假（4 日以上）'],
  ['2026-02-17', '春節連假（4 日以上）'],
  ['2026-02-18', '春節連假（4 日以上）'],
  ['2026-02-19', '春節連假（4 日以上）'],
  ['2026-02-20', '春節連假（4 日以上）'],
  ['2026-02-21', '春節連假（4 日以上）'],
  ['2026-02-27', '和平紀念日彈性放假'],
  ['2026-02-28', '和平紀念日'],
  ['2026-04-03', '兒童節／清明連假（4 日以上）'],
  ['2026-04-04', '兒童節／清明連假（4 日以上）'],
  ['2026-04-06', '清明連假補假（4 日以上）'],
  ['2026-05-01', '勞動節'],
  ['2026-05-02', '勞動節連假'],
  ['2026-06-19', '端午連假'],
  ['2026-06-20', '端午節'],
  ['2026-09-25', '中秋節（連假 4 日以上）'],
  ['2026-09-26', '中秋連假（4 日以上）'],
  ['2026-09-28', '教師節（連假 4 日以上）'],
  ['2026-10-09', '國慶日補假'],
  ['2026-10-10', '國慶日'],
  ['2026-10-24', '光復節連假'],
  ['2026-10-26', '光復節補假'],
  ['2026-12-25', '行憲紀念日'],
  ['2026-12-26', '行憲紀念日連假']
];
